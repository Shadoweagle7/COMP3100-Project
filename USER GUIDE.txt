Run client using java (e.g. if in the same directory as Client.java and Client.class, run java Client). The port is hard coded to 50000. The username is hardcoded to shadoweagle7.

Test scripts are not included in this submission.
